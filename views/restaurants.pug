extends layout

block content
  .container
    .row
      h4 #{titulo}
      .divider
    .row
      each item in dataR
        .col.s12.m6.l4
          .card.small 
            .card-image
              img.materialboxed(src='images/platillos/'+item.imagenPortada,alt='Restaurante')
            .card-content
              span.card-title.activator.grey-text.text-darken-4= item.nombre
                i.material-icons.right more_vert
              p= item.nombre
              p.truncate= item.descripcion
            .card-reveal
              span.card-title.grey-text.text-darken-4 Descripción
                i.material-icons.right close
              p= item.descripcion
            .card-action
              a(href="#") Ver perfil




